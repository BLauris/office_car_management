%table.table.table-hover
  %thead
    %tr
      %th #
      %th First Name
      %th Last Name
      %th E-mail
      %th Created At
  %tbody
    - @users.each do |user|
      %tr 
        %td
          = user.id
        %td
          = user.first_name
        %td
          = user.last_name
        %td
          = user.email
        %td
          = user.created_at.strftime("%d/%m/%Y")